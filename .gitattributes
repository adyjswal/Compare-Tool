# Normalize all text files to LF in the repository so the codebase stays
# consistent across Windows / macOS / Linux (the engine is meant to be portable).
* text=auto eol=lf

# Keep these binary-safe (never munge line endings).
*.png binary
*.jpg binary
*.vsix binary
