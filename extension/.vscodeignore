# Everything here is excluded from the packaged .vsix.
# We ship only the bundled output (dist/) + manifest + docs.
.vscode/**
src/**
webview/**
test/**
scripts/**
node_modules/**
esbuild.js
tsconfig.json
**/*.ts
**/*.tsx
**/*.map
.gitignore
